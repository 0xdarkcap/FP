This zip contains two folder folder "App" represents frontend web application and server is for backend.

Instructions for running backend:
1. Install all dependencies - command is npm Install
2.to run - node app.js

Instructions for running frontend:
1. Install all dependencies - command is npm Install
2.to run - npm run dev
